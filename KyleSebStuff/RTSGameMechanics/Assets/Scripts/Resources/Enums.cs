namespace RTS {
	public enum ResourceType {
		Money,
		Power
	}

    public enum CursorState {
        Select,
        Move,
        Attack,
        PanLeft,
        PanRight,
        PanUp,
        PanDown,
        Capture
    }
}