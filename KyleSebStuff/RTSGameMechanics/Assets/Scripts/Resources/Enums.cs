namespace RTS {
	public enum ResourceType {
		Money,
		Power
	}

    public enum CursorState {
        Select,
        Move,
        HoverEnemy,
        Attack,
        PanLeft,
        PanRight,
        PanUp,
        PanDown,
        Capture
    }
}